"use client";import { useState } from "react";import { ArrowLeft,Save } from "lucide-react";import Link from "next/link";
export function PatientForm(){const[error,setError]=useState(""),[saving,setSaving]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/patients",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const j=await r.json();setSaving(false);if(!r.ok)setError(j.error);else location.href=`/patients/${j.id}`}return <form className="record-form" onSubmit={submit}><div className="section-card"><div className="section-heading"><div><span className="eyebrow">Identity</span><h2>Patient information</h2></div><p>Fields marked with * are required.</p></div><div className="field-grid three"><label>Patient ID *<input name="patientId" required placeholder="e.g. PT-00001"/></label><label>First name *<input name="firstName" required/></label><label>Middle name<input name="middleName"/></label><label>Last name *<input name="lastName" required/></label><label>Date of birth *<input name="dateOfBirth" type="date" required/></label><label>Sex *<select name="sex" required defaultValue=""><option value="" disabled>Select</option><option value="MALE">Male</option><option value="FEMALE">Female</option><option value="INTERSEX">Intersex</option><option value="PREFER_NOT_TO_SAY">Prefer not to say</option></select></label></div></div><div className="section-card"><div className="section-heading"><div><span className="eyebrow">Contact</span><h2>Contact & emergency details</h2></div></div><div className="field-grid"><label>Contact number *<input name="contactNumber" required/></label><label>Email<input name="email" type="email"/></label><label className="span-2">Address<textarea name="address" rows={2}/></label><label>Emergency contact<input name="emergencyContact"/></label><label>Emergency contact number<input name="emergencyContactNumber"/></label></div></div><div className="section-card"><div className="section-heading"><div><span className="eyebrow">Clinical profile</span><h2>Baseline medical information</h2></div></div><div className="field-grid"><label>Blood type<input name="bloodType" placeholder="e.g. O+"/></label><span/><label>Known allergies<textarea name="allergies" rows={3}/></label><label>Existing conditions<textarea name="existingConditions" rows={3}/></label></div></div>{error&&<p className="form-error">{error}</p>}<div className="form-actions"><Link href="/patients" className="button ghost"><ArrowLeft size={17}/> Cancel</Link><button className="button primary" disabled={saving}><Save size={17}/>{saving?"Registering…":"Register patient"}</button></div></form>}

