import { Logo } from "@/components/logo";import { SetupForm } from "@/components/auth-form";import { prisma } from "@/lib/prisma";import { redirect } from "next/navigation";import { Database,KeyRound,ShieldCheck } from "lucide-react";
export const dynamic="force-dynamic";
export default async function Setup(){try{if(await prisma.clinic.count()>0)redirect("/login")}catch{}return <main className="setup-page"><header><Logo/><span>Initial clinic setup</span></header><div className="setup-layout"><section><span className="eyebrow">Private initialization</span><h1>Create the first administrator.</h1><p>This one-time setup connects ClinicSync to your clinic. Once completed, this route is permanently locked.</p><div className="assurance-list"><div><Database/><span><strong>Empty by design</strong>No sample or invented medical records.</span></div><div><KeyRound/><span><strong>Strong credentials</strong>Passwords are hashed before storage.</span></div><div><ShieldCheck/><span><strong>Traceable access</strong>Security-sensitive activity is recorded.</span></div></div></section><div className="setup-card"><h2>Clinic workspace</h2><p>Enter your organization and administrator details.</p><SetupForm/></div></div></main>}

