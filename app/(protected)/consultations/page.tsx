import { requireUser } from "@/lib/auth";import { prisma } from "@/lib/prisma";import { format } from "date-fns";import { ClipboardPlus } from "lucide-react";import { EmptyState } from "@/components/empty-state";
export default async function Consultations(){const user=await requireUser(["ADMIN","DOCTOR","NURSE"]),scope=user.role==="DOCTOR"?{doctorId:user.doctor!.id}:{};const items=await prisma.consultation.findMany({where:{patient:{clinicId:user.clinicId},...scope},include:{patient:true,doctor:{include:{user:true}},prescriptions:true},orderBy:{consultationDate:"desc"},take:100});return <><div className="page-heading"><div><span className="eyebrow">Clinical encounters</span><h1>Consultations</h1><p>Authorized clinical records connected permanently to each patient.</p></div></div><section className="section-card table-card">{items.length?<div className="responsive-table"><table><thead><tr><th>Date</th><th>Patient</th><th>Doctor</th><th>Chief complaint</th><th>Diagnosis</th><th>Prescriptions</th></tr></thead><tbody>{items.map(c=><tr key={c.id}><td>{format(c.consultationDate,"MMM d, yyyy")}<small>{format(c.consultationDate,"h:mm a")}</small></td><td>{c.patient.firstName} {c.patient.lastName}<small>{c.patient.patientId}</small></td><td>Dr. {c.doctor.user.lastName}</td><td>{c.chiefComplaint}</td><td><strong>{c.diagnosis}</strong></td><td>{c.prescriptions.length}</td></tr>)}</tbody></table></div>:<EmptyState icon={ClipboardPlus} title="No consultations recorded" message="Doctors can create a consultation from an assigned appointment."/>}</section></>}

