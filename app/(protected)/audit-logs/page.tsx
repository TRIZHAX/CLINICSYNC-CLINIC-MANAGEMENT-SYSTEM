import { requireUser } from "@/lib/auth";import { prisma } from "@/lib/prisma";import { ShieldCheck } from "lucide-react";import { EmptyState } from "@/components/empty-state";import { format } from "date-fns";
export default async function AuditLogs(){await requireUser(["ADMIN"]);const items=await prisma.auditLog.findMany({include:{user:true},orderBy:{createdAt:"desc"},take:100});return <><div className="page-heading"><div><span className="eyebrow">Security & accountability</span><h1>Audit log</h1><p>Immutable activity records for administrative review.</p></div></div><section className="section-card table-card">{items.length?<div className="responsive-table"><table><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Related record</th><th>IP address</th></tr></thead><tbody>{items.map(a=><tr key={a.id}><td>{format(a.createdAt,"MMM d, yyyy")}<small>{format(a.createdAt,"HH:mm:ss")}</small></td><td>{a.user?`${a.user.firstName} ${a.user.lastName}`:"System"}<small>{a.user?.email}</small></td><td><code>{a.action}</code></td><td>{a.entityType||"—"}<small>{a.entityId}</small></td><td>{a.ipAddress||"Not available"}</td></tr>)}</tbody></table></div>:<EmptyState icon={ShieldCheck} title="No audit events recorded" message="Logins and record changes will appear here as they occur."/>}</section></>}

