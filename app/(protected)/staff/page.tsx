import { requireUser } from "@/lib/auth";import { prisma } from "@/lib/prisma";import { UsersRound } from "lucide-react";import { EmptyState } from "@/components/empty-state";import { UserStatusButton } from "@/components/user-status-button";
export default async function Staff(){const admin=await requireUser(["ADMIN"]);const users=await prisma.user.findMany({where:{clinicId:admin.clinicId},select:{id:true,firstName:true,lastName:true,email:true,phone:true,role:true,status:true,createdAt:true},orderBy:[{role:"asc"},{lastName:"asc"}]});return <><div className="page-heading"><div><span className="eyebrow">Access control</span><h1>Users & staff</h1><p>Manage active clinic accounts and role assignments.</p></div></div><section className="section-card table-card">{users.length?<div className="responsive-table"><table><thead><tr><th>User</th><th>Role</th><th>Email</th><th>Phone</th><th>Status</th><th>Action</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td><strong>{u.firstName} {u.lastName}</strong></td><td>{u.role}</td><td>{u.email}</td><td>{u.phone||"—"}</td><td><span className={`status status-${u.status.toLowerCase()}`}>{u.status}</span></td><td>{u.id!==admin.id&&<UserStatusButton id={u.id} status={u.status}/>}</td></tr>)}</tbody></table></div>:<EmptyState icon={UsersRound} title="No user accounts" message="Create authorized clinic accounts to manage staff access."/>}</section></>}

