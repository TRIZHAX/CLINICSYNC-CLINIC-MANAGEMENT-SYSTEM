import { requireUser } from "@/lib/auth";import { prisma } from "@/lib/prisma";import { Bell,CalendarDays,ClipboardCheck } from "lucide-react";import { EmptyState } from "@/components/empty-state";import { formatDistanceToNow } from "date-fns";
export default async function Notifications(){const user=await requireUser();const items=await prisma.notification.findMany({where:{userId:user.id},orderBy:{createdAt:"desc"},take:100});return <><div className="page-heading"><div><span className="eyebrow">Account updates</span><h1>Notifications</h1><p>Appointment and care updates created by real clinic activity.</p></div></div><section className="section-card notification-list">{items.length?items.map(n=><article key={n.id} className={!n.readAt?"unread":""}><span className="notification-icon">{n.type==="APPOINTMENT"?<CalendarDays/>:<ClipboardCheck/>}</span><div><h3>{n.title}</h3><p>{n.message}</p><small>{formatDistanceToNow(n.createdAt,{addSuffix:true})}</small></div>{!n.readAt&&<i/>}</article>):<EmptyState icon={Bell} title="No notifications" message="Updates from real appointment and consultation activity will appear here."/>}</section></>}

