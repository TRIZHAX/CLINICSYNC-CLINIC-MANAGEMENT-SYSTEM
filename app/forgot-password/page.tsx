"use client";import { useState } from "react";import Link from "next/link";import { Logo } from "@/components/logo";import { ArrowLeft,Mail } from "lucide-react";
export default function ForgotPassword(){const[message,setMessage]=useState(""),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const email=new FormData(e.currentTarget).get("email");const r=await fetch("/api/auth/password-reset/request",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setMessage((await r.json()).message||"Request received.");setLoading(false)}return <main className="center-page"><div className="message-card" style={{textAlign:"left"}}><Logo/><span className="eyebrow" style={{display:"block",marginTop:28}}>Account recovery</span><h1>Reset your password</h1><p>Enter your account email. If it exists, your configured mail provider will send a time-limited reset link.</p><form className="auth-form" onSubmit={submit}><label>Email address<div className="input-with-icon"><Mail size={18}/><input type="email" name="email" required/></div></label>{message&&<p className="form-error" style={{color:"var(--brand)"}}>{message}</p>}<button className="button primary wide" disabled={loading}>{loading?"Submitting…":"Request secure reset"}</button></form><Link href="/login" className="button ghost" style={{marginTop:12}}><ArrowLeft size={16}/>Back to sign in</Link></div></main>}

